// An array is a list-like structure that stores multiple values in a single variable.

// Values can be of any type: numbers, strings, booleans, objects, even other arrays.

//------------------syntax

let arr= [10,20,30,40,50];
console.log(arr[0]);
console.log(arr[1]);
console.log(arr.length);



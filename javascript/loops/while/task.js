// Keep asking the user for a number until they enter 0.

let i = Number(prompt("Enter the nuumber:"));
while (i !== 0) {
 
    i = Number(prompt("Enter the number: "));
  
}
console.log("You entered 0 , exiting");
